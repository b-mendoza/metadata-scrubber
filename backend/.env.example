# Populate all R2 settings in every local, preview, production, test,
# health-only, and scrub-only runtime before deploying this backend version.
PORT=8080
R2_ACCESS_KEY_ID=synthetic-access-key-id
R2_BUCKET=metadata-scrubber-example
R2_ENDPOINT=https://0123456789abcdef0123456789abcdef.r2.cloudflarestorage.com
R2_SECRET_ACCESS_KEY=synthetic-secret-access-key
