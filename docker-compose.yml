services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgres://user:password@localhost:5432/database
      HOST: 0.0.0.0
      PORT: 3000
    ports:
      - "3000:3000"
