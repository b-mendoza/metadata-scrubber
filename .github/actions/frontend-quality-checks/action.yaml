name: Code Quality Checks

description: "Runs linting, formatting, and type checks to ensure code quality"

runs:
  using: composite
  steps:
    - name: "Run linting, formatting, and type checks"
      shell: bash
      working-directory: frontend
      run: pnpm run lint
