{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    ":automergeMinor",
    "config:best-practices",
    ":pinAllExceptPeerDependencies"
  ],
  "lockFileMaintenance": {
    "enabled": true
  },
  "packageRules": [
    {
      "description": "ai and @ai-sdk/* share types across versions; they break when updated separately",
      "groupName": "AI SDK packages",
      "matchPackageNames": ["/^ai$/", "/^@ai-sdk\//"]
    },
    {
      "description": "drizzle's beta tags confuse renovate into proposing downgrades",
      "enabled": false,
      "matchPackageNames": ["drizzle-kit", "drizzle-orm"]
    },
    {
      "description": "backend/go.mod and backend/Dockerfile declare one Go toolchain version; CI and the container build fail when the two update separately",
      "groupName": "Go Docker image and go.mod",
      "matchManagers": ["dockerfile", "gomod"],
      "matchPackageNames": ["golang", "go"],
      "matchUpdateTypes": ["major", "minor", "patch"],
      "minimumGroupSize": 2
    },
    {
      "description": "Renovate treats the go.mod go directive as a minimum version and does not bump it by default. Enable bumps so pull requests update it to the latest Go release.",
      "matchDepNames": ["go"],
      "matchDepTypes": ["golang"],
      "matchManagers": ["gomod"],
      "rangeStrategy": "bump"
    },
    {
      "description": "By default Renovate only runs `go get`, which can leave go.sum without full-content checksums for shared transitive deps needed by go.mod `tool` directives (broke CI on the pdfcpu v0.14.0 PR). Run `go mod tidy` so non-major gomod PRs ship a consistent module graph (Renovate skips tidy on major updates unless gomodUpdateImportPaths is also enabled).",
      "matchManagers": ["gomod"],
      "postUpdateOptions": ["gomodTidy"]
    }
  ],
  "rebaseWhen": "conflicted",
  "reviewers": ["b-mendoza"]
}
